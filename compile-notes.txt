cmake -DCMAKE_CXX_FLAGS="-I ${CONDA_PREFIX}/include" ../
cmake -DCMAKE_BUILD_TYPE=Debug -DCMAKE_CXX_FLAGS="-I ${CONDA_PREFIX}/include -L ${CONDA_PREFIX}/lib" ../
cmake -DCMAKE_CXX_FLAGS="-I ${CONDA_PREFIX}/include" ../ && cmake --build . && cmake --install . --prefix ../install/ 


# most recent
cmake -DCMAKE_BUILD_TYPE=Debug -DCMAKE_CXX_FLAGS="-I ${CONDA_PREFIX}/include -L ${CONDA_PREFIX}/lib -Wl,-rpath-link=${CONDA_PREFIX}/lib" -S ../ -B .

cmake --build .
cmake --install . --prefix ${CONDA_PREFIX}/

####
On Ubuntu without virtualenv need to set linker directory to run
LD_LIBRARY_PATH=/opt/lib/ ./FastaTest
ldd on the artifacts shows no hard coded link. Makes sense that past versions required -Wl,-rpath-link to work.

May make sense to just add /opt/lib to .bashrc because this is my general library install location. Could also make sense to statically link exe <- hts-wrapper.a <- libhts.a
